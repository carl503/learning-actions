[project]
name = "learning-actions"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Carl Lubojanski", email = "carl.lubojanski@outlook.com" }
]
requires-python = ">=3.13"
dependencies = []

[project.scripts]
learning-actions = "learning_actions:main"

[build-system]
requires = ["uv_build>=0.12.11,<0.13.0"]
build-backend = "uv_build"

[dependency-groups]
dev = [
    "pytest>=9.1.1",
]
